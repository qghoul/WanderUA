spring:
  datasource:
    url: jdbc:h2:mem:testdb

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

  redis:
    enabled: false;

    web:
      resources:
        add-mappings: false

jwt:
  secret: test-test-test-test-test-test-test-test

app:
  uploads:
    base-path: src/test/resources/uploads